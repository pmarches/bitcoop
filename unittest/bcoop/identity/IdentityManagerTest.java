package bcoop.identity;

import java.io.File;
import java.io.IOException;

import mockObject.MockIdentityManager;

import junit.framework.TestCase;

public class IdentityManagerTest extends TestCase {

	/*
	 * Test method for 'bcoop.identity.IdentityManager.IdentityManager(String)'
	 */
	public void testIdentityManager() throws IOException {
//		IdentityManager idManager = new MockIdentityManager(MockIdentityManager.CLIENT_IDENTITIES_PATH);
//		assertEquals(2, idManager.countIdentities());
//		Long[] uidArray = idManager.getUidArray();
//		assertEquals(2, uidArray.length);
//		assertTrue(uidArray[0]!=uidArray[1]);
//		Identity identity1 = idManager.getIdentity(uidArray[0]);
//		Identity identity2 = idManager.getIdentity(uidArray[1]);
//
//		assertEquals(uidArray[0], identity1.getUniqueId());
//		assertEquals(uidArray[1], identity2.getUniqueId());
//
//		File tFile = new File(MockIdentityManager.CLIENT_IDENTITIES_PATH);
//		assertTrue(tFile.exists());
//		assertTrue(tFile.length() != 0);
	}
}
